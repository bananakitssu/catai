name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Install test dependencies
        run: python -m pip install --upgrade pip pytest
      - name: Install CatAI
        run: python -m pip install -e .
      - name: Run tests
        run: python -m pytest -q

  generate:
    needs: test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Install CatAI
        run: python -m pip install -e .
      - name: Download real training corpus
        run: python data/download_corpus.py --output data/tinyshakespeare.txt
      - name: Train model
        run: |
          python -m catai \
            --corpus data/tinyshakespeare.txt \
            --checkpoint catai.pt \
            --epochs 1 \
            --batch-size 32 \
            --sequence-length 64 \
            --d-model 32 \
            --heads 4 \
            --layers 2 \
            --device cpu
      - name: Generate text
        run: |
          python -m catai.generate_cli \
            --checkpoint catai.pt \
            --prompt "The cat" \
            --max-new-tokens 200 \
            --temperature 0.8 \
            --repetition-penalty 1.1 \
            --device cpu
